<!--rewards -->
<div x-cloak :class="rewardsPanelOpen ? 'opacity-[1] !z-[1001]' : 'opacity-0 pointer-events-none'" class="transition-opacity flex fixed top-0 left-0 w-screen h-screen z-[-1]">

    <div :class="rewardsPanelOpen ? 'opacity-1' : 'opacity-0'" class="flex flex-col bg-white m-auto w-full max-w-[500px] h-full max-h-[500px]  border border-green z-[2]">
        <div class="hidden flex p-[30px] border-b border-green">
            <span class="text-[40px] leading-[40px] uppercase">Rewards</span>
            <button class="text-[19px] uppercase pt-[12px] pb-[9px] px-[22px] border border-green text-center ml-auto hover:bg-green hover:text-white transition-all">See Rewards</button>
        </div>

        <div class="hidden flex flex-col px-[30px] py-[60px] gap-y-[60px]">
            <div class="flex flex-col gap-y-[19px]">
                <span>Create an account for faster checkout and start earning your RAW Rewards! Earn 2000 points for VIP status and earn double points.</span>

                <a href="/account/register" class="bg-green text-white text-center py-[18px] px-[20px] text-[18px] uppercase mt-[10px]">Create Raw Account</a>
                <span class="text-[16px]">Already have an account? <span class="underline">{{ 'Sign in' | customer_login_link }}</span></span>
            </div>
        </div>

        <iframe class="w-full h-full" src="https://app.marsello.com/Portal/Custom/Welcome/5a11ff028e2bf303e4e8f631?aProvId=5a11fee18e2bf308c8686b42" frameborder="0"></iframe>
    </div>

    <div x-cloak @click="rewardsPanelOpen = false" :class="rewardsPanelOpen ? 'opacity-[0.3]' : 'opacity-0 pointer-events-none'" class="fixed transition-opacity top-0 left-0 w-screen h-screen bg-black opacity-[0.3] z-[1]"></div>
</div>
